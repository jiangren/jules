## 2026-04-12 - Focus Visible in CSS Modules
**Learning:** Default browser focus outlines can sometimes be masked or insufficient when using CSS Modules and complex styling. It is critical to explicitly add `&:focus-visible` to ensure high-contrast focus rings for keyboard accessibility.
**Action:** Always include an explicit `&:focus-visible` state when styling custom buttons or links.
